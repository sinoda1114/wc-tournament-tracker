# タスク台帳（永続・セッション横断）

**🕒 最終更新: 2026-06-14 JST** — 番人マージ待ち=**PR #46 のみ**。本番反映済=#78(T-62引き分け結果取りこぼし修正＋本番DB migration0016適用済)・#80(T-73投票締切「移行中/全終了」出し分け)・#79(T-72スマホナビ横スクロール発見性フェード)・#77(T-69文言修正)・#76(T-69投票ガイド/T-70予想注記中立化)・#75(T-71予想リスト同点同順位)・#74(T-63同点同順位/T-65出場停止🚫)・#73(T-64日付一覧グループ名)・#72(T-66スマホ右上⋮集約)・#71(T-67 A2HSショートカット手順案内)・#70(T-60 A2HSアイコン)・#69(T-59ブラケット線色均一化)・#68(T-61残 PCナビ⭐左)・#67(T-61ナビ⭐左/フッター隠しバー/帰属移設)・#66(T-58スマホ第3弾)・#64(T-57スマホ第2弾)・#65(T-7 CI緑化)・#63(T-56)・#62(T-55)・#61(T-54)・#60(T-53注釈)・#59(postcss)・#58(T-50勝者色)・#57(T-48/T-51)・#54(T-19)。

> **運用**（[[numbered-choices]] 準拠）
> - 番号 **T-N は固定**。振り直さない。新規は「台帳の最大+1」を台帳に追記して確保。
> - 完了は ~~取り消し線~~ ＋完了日・PR を残す。**完了したら「着手可」から下の「✅完了」へ移す**（現役セクションに残さない＝埋もれ防止）。
> - **正本はこのファイル**。セッション/ハーネスの TaskList 表示は揮発・ドリフトするので信用しない（食い違ったら台帳が正）。
> - **【共通ルール｜GitHub 一本化】正本は GitHub `main` 上の本ファイル**。ローカルはミラー。変更したら都度すぐ GitHub `main` へ push 同期（マークダウン単一ファイルのコミットで可）。読むときは `git show origin/main:notes/task-ledger.md` を正とする。
> - **【共通ルール｜内容クロバー防止｜2026-06-14 確立】台帳を `gh api` で更新する直前に、必ず最新の中身を取り直し（`gh api …/contents/notes/task-ledger.md` の content＋sha）、その**最新の中身に差分挿入**してから PUT する。記憶や古いローカルコピーから**全文を再生成して PUT しない**。理由＝Contents API の `sha` 楽観ロックは **sha しか守らない**ので、古い base で組んだ全文を“PUT直前に取り直した最新 sha”で出すと sha 一致で通ってしまい、**その間に入った他者の編集が丸ごと消える＝内容クロバー**（実例: T-69 が番人の上書きで一度消失 `1b6f919` で復旧）。PUT 後は必ず `diff` で「ローカル＝GitHub一致」を検証する。[[ledger-github-single-source]]・[[tasklist-ledger-sync]]。
> - **PR の「マージした/してない」は `gh pr list --state open` が正本**（記憶・伝聞で語らない）。
> - **表記**: タスクは `T-39`。PR は**必ず `PR #19` と明記**（`#19` 単独禁止）。コミット/PR文では task に `#` を使わない。
> - **優先度表記**: 「🔴 最優先」「🟠 次」「🟡 その後」（"P1/P2" は廃止）。
> - **同期**: 番人⇄エージェントは `notes/agent-sync.md` のプロトコル。W系・AT系は [launch-task-board.md](launch-task-board.md)。

---

## 📊 いまの状態

- **本番**: **https://matchfav.com** ✅（独自ドメイン・本番 Clerk live）。デプロイは git 自動（push→Preview / main マージ→Production）。
- **オープンPR の正本**: `gh pr list --state open`。

## 📌 番人マージ待ち PR ＝ いまの主戦場

| PR | タスク | 残り条件（誰待ち） |
|---|---|---|
| **PR #46** | T-14/T-52 Stripe＋決勝T課金壁 | 🔴 **篠田**: Stripe設定＋env6種 → 本番DB migration0013 → マージ（詳細↓「🔴 T-14」） |

## 🌙 夜間に回せるタスク（自走候補・2026-06-14 篠田就寝前に選別）

> 「篠田の判断/入力が要らず、worktree で自走→朝に PR が出せる」もの。実装は worktree 側（main/タスク管理dir は不可侵）。**起動には篠田の明示GOが必要**。

- ~~**① 🟢 T-72 スマホ上部ナビの発見性（右端フェード＋半見せ）**~~ → ✅完了（PR #79・6/14本番反映）。
- ~~**② ✅ T-62（PR #78）引き分け結果の取りこぼし**~~ → **✅完了（2026-06-14）**: `/ai-review`（致命バグ=migration0016のmatch_events退避漏れ修正）→ `/security-review`（High/Medium無し・マージ可）→ **PR #78 マージ（`2d8f73a`）→ 本番DB migration 0016 適用済**（0016のみExecuted・他Skip・match_events66件/bracket_edges32件 保全確認）。取込dry-runで **#3→1-1 finished の更新が計画される**ことを実証（旧CHECKで弾かれていたケースが通る）。**#3 の実反映は次cron自動回復待ち**（手動`--apply`はガードでブロック＝承認あれば即時可）。

### ⛔ 夜間に回せない（篠田待ちでブロック）
- **T-47** ヒーロー文言 — 篠田の**変更後テキスト**が無いと書けない。
- **T-14 / PR #46** Stripe — 篠田のオーナー作業（Stripe設定＋env6種）が前提。
- **T-68** 決勝T課金壁の拡張 — #46 マージ後でないと載らない。

### 起動メモ（篠田のGO待ち）
- 「T-72 回して」→ worktree 立てて実装エージェント起動。
- 「T-62 事前承認OK」→ 番人に security-review→マージ→migration0016適用を委任（本セクション/agent-sync に記録）。

## 🎯 次やること（PR待ちでない・着手可）

- **T-47** ヒーロー文言変更 — 篠田の**変更後テキスト指定待ち**（`home.title`/`description` ×5言語）。ui-feature・worktree→PR。
- **🟠 T-76** 得点ランキングからチェコの得点者（クレイチ Krejčí）が欠落 — **事象（2026-06-14 篠田・Yahooスポナビ突合）**: `/rankings` 得点ランキングを Yahoo と1行ずつ突合した結果、**14人中13人＋1位 Balogun 2点・同点同順位は完全一致**だが、**試合 #2「韓国 2-1 チェコ」のチェコの1点の個人得点者（Yahoo＝ラディスラフ・クレイチ Ladislav Krejčí）が我々のランキングに存在しない**。**裏付け**: 確定6試合の総得点=16 に対し我々の得点ランキング合計=2+(13×1)=**15**＝差の1がこのチェコ得点。**スコア(2-1)・順位表(チェコ 得=1)は正しく、壊れているのは個人得点者の goal イベント紐付けのみ**（[[T-62]] はスコア欠落＝別症状）。**原因候補**: ①Wikipedia取込時にチェコ得点者名が拾えず/選手名簿(`player_clubs`等)と紐付かず goal イベント欠落 ②我々のデータで own_goal 扱い（オウンゴールは得点者ランキング非表示）→Yahooの「クレイチ得点」と齟齬。**切り分け**: (a) 篠田に現実確認＝チェコの1点はクレイチの得点か/オウンゴールか (b) admin/DBで #2 の match_events を確認（チェコ側 goal イベントの有無・type・player紐付け）(c) Wikipedia該当ページの得点者表記と取込ロジック突合。**集計ロジック自体(`aggregateScorers`)は正常**（他15点は正しく紐付く）＝**データ/取込側の問題**。data-squad/ingest 領域・worktree→PR。（2026-06-14 起票）
  - **✅ 真因確定（2026-06-14 Claude調査・Wikipedia原文＋パーサ照合／篠田が現実=クレイチの正規ゴール[後半14分=59']とYahooで確認・オウンゴールではない）**: 現在の Wikipedia「2026 FIFA World Cup Group A」KOR vs CZE box の `goals2` に Krejčí の得点は**実在**＝`[[Ladislav Krejčí (footballer, born 1999)|Krejčí]] 59'`。**ところが韓国側 goals1 は各行が `*` 始まり（`*[[Hwang In-beom]] 67'` 等）なのに、チェコ goals2 の唯一の得点行は先頭 `*`（箇条書き記号）が無い**（Wikipedia編集者は得点が1件のとき `*` を省くことがある）。我々の `parseGoals`（`src/lib/ingest/wikipedia.ts`）は **`if (!line.startsWith('*')) continue;`** で `*` 始まりでない行を捨てるため、**この1行が丸ごとスキップ→チェコ得点イベント未取込**。結果、**試合詳細タイムライン（#2 のできごとにチェコ得点が無い＝篠田スクショで確認）と得点ランキングの双方から欠落**（同一の単一原因）。スコア(2-1)が正しいのは結果が TheSportsDB 由来だから。**＝システム的パーサバグ**（今後も「得点1件で `*` 省略」の試合は同様に欠落する）。**修正方針**: `parseGoals` の `*` 必須要件を緩和し、goals 値内で **`[[link]]`＋分** を持つ行は `*` 有無に関わらずパース（空行のみスキップ・over-capture に注意）。`tests/fixtures/wikipedia/` に **`*` 無し単独得点** のケースを追加して回帰担保。修正＆ #2 再取込で Krejčí 反映。**昇格：🟠→実害あり（公開データの欠落）として優先度上げ。担当=data-squad/ingest・worktree→PR。**
- **🟡 T-75** 本番500スパイクの事後確認（Vercelアラート `/[locale]`）— **事象（2026-06-14 篠田・Vercelメール）**: Vercel anomaly「Medium Severity: 500 errors on multiple routes」**Started 2026-06-14 00:40 UTC（＝09:40 JST）**・route `/[locale]`・2 anomalies・"5xx status codes increased"。**現状（Claude調査 2026-06-14 ~11:58 JST）＝復旧済み・継続障害なし**: 全ルート200（/ /en /ja /prediction /rankings）・/matches/3（引分1-1）200・トップ /en 連打10回すべて200・引き分け表示（"draw"/"終了"）正常レンダリング・**再現せず**。**タイミング分析**: 00:40 UTC にデプロイ無し（夜間マージ #78/#79/#80 は前日 UTC夕方＝JST深夜2-3時で約6h前）→ **コード投入起因ではなくランタイム/データ/DB起因**。**仮説候補**: ①**migration 0016 の matches テーブル再作成（DROP OLD→RENAME）窓の最中に matches クエリが500**＝[[T-74]] の失敗モードそのもの（⚠️ただし0016適用は #78マージ ~18:13 UTC前日で00:40 UTCと約6hズレ→再適用 or Turso edge replica 伝播の可能性）②外部API（TheSportsDB/天気）障害で SSR `/[locale]` が500 ③Turso 瞬断。**真因確定の唯一手段＝Vercel ダッシュボードの該当ログ**（`/[locale]` 00:40 UTC のスタックトレース）を番人が確認。**対策**: 真因が①なら **T-74（migrate Tx化）を前倒し**＋本番migration適用は低トラフィック時/メンテ告知で。緊急ではない（復旧済）が再発監視。**担当=番人（ログ確認）**。（2026-06-14 起票）
  - **✅ データ整合性 検証クリーン（2026-06-14・Claude機械検査＋篠田オーナー確認）**: 確定6試合（#1メキシコ2-0南ア/#2韓国2-1チェコ/#3カナダ1-1ボスニア/#4アメリカ4-1パラグアイ/#7ブラジル1-1モロッコ/#8カタール1-1スイス）について、(a)スコア↔勝敗の矛盾ゼロ (b)引分3件すべて同点1-1・幽霊勝者なし (c)**順位表＝スコアから独立再計算した値と完全一致**（テーブル再作成での列ズレ/破損なし）。**篠田が現実の結果と照合し「引分含め全部正しい」と確認**＝**500スパイクによるデータ破損は無し**。→ T-75 は「データ復旧案件」ではなく**純粋に真因をVercelログで確認するだけ**に格下げ。**残watch**: #5（ハイチvsスコットランド・6/14 10:00JST KO）が検査時点で結果未取得＝cron遅延の正常範囲。次cron（04:00 UTC〜）後に反映確認。
- ~~**🟠 T-72** スマホ上部ナビの横スクロール発見性（右端フェード＋次項目の半見せ）~~ → ✅**完了（PR #79・6/14本番反映）**。以下は原票↓ — **事象（2026-06-14 篠田・スクショ）**: スマホでナビが `⭐｜グループリーグ｜決勝T｜出場国` で**右端がスパッと切れ**、その先の**優勝予想・ランキングが画面外にあるのにスクロール可能の手がかりが無い**＝「出場国まで」しか無いように見え、重要導線（優勝予想/ランキング）に気づけない。**制約（篠田明言）**: 文字を小さくして全部詰め込むのは不可（タップ性が落ち体験が悪化＝NG）。**採用案（A・篠田選択 2026-06-14）＝右端グラデーションフェード＋次ピルの半見せ**: ①横スクロールコンテナ右端に背景→透明のフェード（`mask-image: linear-gradient(to right,#000 ~85%,transparent)` or `::after` グラデ）で「まだ右に続く」を示唆 ②右 padding/幅調整で**最後の可視ピルが端で半分だけ覗く**ようにする（切れている＝続くの最強シグナル）③`scrollLeft` 末尾到達でフェードを外す（端に来たと分かる・スクロール位置で両端フェードを出し分けると尚良い）④任意で `scroll-snap` で操作感向上。**タップ領域・文字サイズは不変**（既存ピルのまま）。**スマホのみ・PC不可侵**（PCは全項目見えており対象外＝[[方針: モバイル最適化はPC不可侵]]）。対象 `SiteNav`／`globals.css`。検証＝実機 320/375/390＋ライト/ダーク＋「端到達でフェード消滅」。ui-feature 領域・worktree→PR。（2026-06-14 起票）
- ~~**T-62 PR #78**~~ → **✅完了（2026-06-14・本番migration0016適用済・#3は次cron自動回復）**。試合結果（スコア/終了）の取りこぼし — **🔬調査で真因確定（当初仮説は誤り）**: TheSportsDB は当該結果(1-1)を保持＝遅延/欠落ではない。真因は**「終了試合は winner 必須」の制約（アプリ`updateMatchResult`＋DB CHECK の二層）が引き分け（勝者不在が正常）を弾く**こと。cron がスコア/finishedを取りこぼし、イベント同期は独立に走るため「イベントだけ入る」。修正(PR #78): group_stage 引分は winner=null 許可（KOは winner 必須維持）＋migration 0016 で DB CHECK を「終了=(勝者あり)or(同点)」へ緩和＋TDDテスト（全469PASS）。⚠️無料キーはタイムライン5件切詰のため「イベントからスコア導出」案は不採用が正解。**残**: /security-review → マージ → 本番DB migration0016 適用(要GO) → 次cronで自動回復。以下は原票↓ — 原因究明＋**再発防止** — **事象（2026-06-14 篠田）**: 試合 #3（カナダ vs ボスニア・6/13 04:00・~20h経過）が **イベント（得点 21'Lukić/78'Larin・カード・交代）は入っているのにスコア/「終了」が未反映**（matchfav.com/matches/3）。**原因の当たり（Claude調査 2026-06-14）**: 取込は **結果＝TheSportsDB（`run.ts`の`provider.fetchResults()`→`updateMatchResult`）／イベント＝Wikipedia の別ソース**。cron は毎時発火・Bosnia 別名解決（`bosnia herzegovina→bih`）も正常＝突合失敗ではない疑い。→ **TheSportsDB 無料枠が当該試合の結果を未掲載/遅延すると、Wikipedia にゴール情報があってもスコア/finished が設定されない**＝**結果が TheSportsDB 単一ソース依存**で無料枠の遅延/欠落がそのまま取りこぼしに。**まず原因確定**: TheSportsDB が本当に #3 を未掲載か／cron ログ／planMatchUpdates の突合結果を実データで確認。**再発防止案**: ①**Wikipedia（取込済イベント）からスコア/finished をフォールバック導出**（goal イベントをチーム別集計→score・試合終了で finished。**manual tier 不可侵は維持**）②or **KO+Nh 経過しても finished にならない試合を検知してアラート/手動tier補完**（admin）③TheSportsDB の ID/日付マッチング再確認。data-squad/ingest 領域・worktree→PR。（2026-06-14 起票）
- **🔴 T-68** 決勝T課金壁の拡張（T-14マップ確定分・PR #46 の投票ゲートに上乗せ）— **方針（2026-06-14 篠田確定）**: 決勝T期間（6/29〜）の未購入者に対し、PR #46（T-52=投票ゲート）に加えて以下も `isKnockoutStage(current) && !hasKnockoutAccess()` で fail-closed にする。①カード表示ビュー（ブラケット⇄カード切替のカード側）②日付フィルタ操作（昨日/今日/明日/明後日/カレンダー＝`DateFilterBar`）③試合詳細 `/matches/[id]`（既存 T-45 の登録ウォールとは別レイヤ＝購入ウォール）④お気に入り（決勝T追跡）⑤優勝予想の深い機能（みんなの予想/成績の押下）。**無料で残すもの＝ブラケット（盤面スコア込み）/アーカイブ（過去結果の素の一覧）/ランキング**。**UX必須**: 各壁に「🔒 決勝Tをアンロック ¥980」CTA（PaywallLock 流用）。グループステージ中は全機能無料を維持（回帰させない）。auth-billing 領域・[[launch-monetization-plan]]・[[voting-monetization-model]]。**実装担当=番人**（着手前に T-14 マップ＝方針決定セクションを必読）。
- **T-54 残** — ③ GSC 登録・インデックス送信（**篠田**）。コード（JSON-LD/メタ/llms.txt）は #61 で本番反映済。
- **T-53 残** — ① 補充選手の**本番DB手動反映**（要GO・選手データ研究・任意。注釈② は #60 で対応済）。
- **T-12** サイト全体の最終動作チェック（実装出揃い後 → 2段ゲート → デプロイ）。

## 🧊 バックログ（低優先・今やらなくてよい）

- **T-74** migrate ランナーのトランザクション化（マイグレーション原子性）— **背景（2026-06-14・PR #78 ai-review 指摘）**: `scripts/migrate.ts` が各 migration を**トランザクション無し**で逐次実行しており、テーブル再作成系（0003/0016 等の CREATE NEW→INSERT SELECT→DROP OLD→RENAME）が**途中で失敗するとスキーマが半壊**してロールバックできないリスク。対策＝各 migration を `BEGIN IMMEDIATE; … COMMIT;`（失敗時 `ROLLBACK`）で包む。⚠️**全 migration 共通の基盤変更**で PR #78 の範囲外＝別タスク。緊急ではない（適用は番人が手で慎重に行うため）が、本番DBスキーマ操作の安全余裕として有効。⚠️SQLite の DDL の一部はトランザクション内でも自動コミットされうるので、libSQL/Turso の挙動を確認のうえ「失敗時に確実に巻き戻る」ことをテストで担保すること。DB/インフラ領域・worktree→PR。（2026-06-14 起票）
- **T-8** セキュリティ仕上げ（Sentry / CSP 方針）。
- **T-35** お気に入り端末間同期の実機 E2E（`notes/e2e-checklist-30-sync.md`）。
- **T-28** 大会汎用化の判断（〜2026-08末）。

---

## ✅ 最近完了（PR・本番反映）

T-62 引き分け結果取りこぼし修正＋本番DB migration0016適用(PR #78・6/14・#3は次cron自動回復) / T-73 投票締切「移行中/全終了」出し分け(PR #80・6/14本番反映) / T-72 スマホナビ横スクロール発見性フェード(PR #79・6/14本番反映・PC不可侵) / T-69 投票ガイド＋T-70 予想注記中立化(PR #76・6/14本番反映) / T-71 予想リスト同点同順位(PR #75・6/14本番反映) / T-63 同点同順位＋T-65 出場停止🚫(PR #74・6/14本番反映) / T-64 日付一覧にグループ名(PR #73・6/14本番反映) / T-66 スマホ右上⋮集約(PR #72・6/14本番反映・PC不変) / T-67 A2HSをショートカット手順案内に統一(PR #71・6/14本番反映・manifest browser化) / T-60 A2HSアイコン スマホ＋プラス(PR #70・6/14本番反映) / T-59 ブラケット接続ライン色の均一化(PR #69・6/14本番反映) / T-61残 ナビ⭐をPCでも最左(PR #68・6/14本番反映) / T-61 ナビ⭐左/フッター隠しバー/Wikipedia帰属を規約へ移設(PR #67・6/13本番反映) / T-58 スマホUI第3弾 ⭐行頭/フッター圧縮(PR #66・6/13本番反映・main CI緑) / T-57 スマホUI第2弾 順位表列圧縮/ナビ/フィルタ左/H1(PR #64・6/13本番反映) / T-7 CI緑化 esbuild 0.28.1(PR #65・6/13・main CI緑 d6c99c5・原因=新advisory公開) / T-56 スマホ画面最適化(PR #63・6/13本番反映・篠田確認待ち) / T-55 スマホヘッダー削減(PR #62・6/13本番反映・篠田OK) / T-54 SEOブランド発見性(PR #61・6/13) / T-53 スカッド注釈6カ国×5言語(PR #60・6/13) / postcss脆弱性 Dependabot#14(PR #59・6/13) / T-50 勝者色 青→金(PR #58・6/13) / T-48 投票リセット自分票のみ＋T-51 グループ投票窓開放(PR #57・6/13本番反映) / T-46 R32再充填修正(PR #56+migration0015・6/13) / T-19 metadata多言語化(PR #54・6/13) / T-43(投票)ライフサイクル(PR #55+migration0014・6/13) / T-45 登録ウォール(PR #47・6/13) / T-43(Wikipedia)イベント補完(PR #43) / T-44 公開ブラウズ案A(PR #42) / チーム詳細404調査クローズ(6/13・誤URL) / T-42 自動取込実証(6/12開幕戦MEX-RSA) / T-22/T-25 本番ドメイン＋本番Clerk(6/12) / T-40 ランキング(PR #28) / T-41 favicon v16(PR #27) / T-33 確定カラー(PR #23) / T-39 お気に入り一掃(PR #19) / T-38 在籍クラブ(PR #16) / T-37 ミニヒーロー(PR #14) / T-36 favicon(PR #12) / T-34 法務簡素化(PR #7) / T-32 順位表フラット(PR #6) / T-31 A2HSラベル(PR #8) / T-26 予告バナー(PR #5)

## 📋 方針決定（やらない/許容と決めたこと）

- **【モバイル最適化はPC不可侵】スマホUI の最適化（T-56/T-57 等）は、PC/デスクトップ表示に一切影響・回帰を出さない**（2026-06-13 ユーザー指示）: PC ブラウザは現状で問題なし。スマホ向け変更は**モバイル幅のメディアクエリ/ブレークポイント内に閉じ込める**（PC 幅は不可侵）。検証時は実機/スマホ幅だけでなく **1024/1440 の PC 表示が無変化**なことも必ず確認する。`clamp()` は上限（PC 幅側）が現状維持になるレンジに収める。今後のスマホ系タスクすべてに適用。
- **/admin の404秘匿化はやらない**（2026-06-13 ユーザー判断）: 探索リスク低・ログインしても非adminは404のため実害なし。
- **【T-14 決勝T課金マップ確定】決勝T期間（6/29〜）の未購入者の可視範囲を確定**（2026-06-14 ユーザー決定）。線引き原則＝「**静的に“見る”＝無料 ／ “操作・ドリルイン・追跡”＝有料**」。
  - ✅ **無料**: ①決勝Tブラケット（盤面の勝敗/スコア込みのチラ見せ＝フック）②アーカイブ（過去結果の素の一覧/結果ページ）③ランキング（得点/カード）
  - 🔒 **有料（買い切りエンタイトルメント）**: ①カード表示ビュー（ブラケット⇄カード切替の**カード側**）②日付フィルタ操作（昨日/今日/明日/明後日/カレンダー）③試合詳細 `/matches/[id]` ④お気に入り（決勝T追跡）⑤みんなの投票（=T-52で実装済）⑥優勝予想の**深い機能**（みんなの予想/成績）。優勝予想そのものは閲覧可、押下=有料。
  - **UX原則（必須）**: 壁は“隠すだけ”にせず、各所で「🔒 決勝Tをアンロック ¥980」の魅力的CTAを出し“買えば開く価値”を見せる（行き止まり感を出さない）。
  - 整合メモ: 「アーカイブ=無料／日付フィルタ・カード表示で掘る=有料」＝**結果データは無料で見れるが、進行中の“操作系（フィルタ/カードビュー）”は有料**。グループステージ中は全機能無料（従来どおり）。実装は **T-68**。
- **【スカッド凍結ルール】大会期間中は `npm run db:fetch-squads` を実行しない**（2026-06-13 ユーザー決定）:
  - `scripts/fetch-squads.ts` はチームごとに DELETE→再INSERT する**破壊型**で、手動更新（遠藤→町野 等）と club_id 紐付けを上書きする。
  - 名簿変更は怪我離脱→補充程度のレアケース。**選手入替が起きたらユーザーが番人に伝え、本番DBを個別に手動更新**（取得スクリプトは改修しない＝YAGNI）。

---

# 詳細（フル説明）

## 🔴 T-14 / T-52 — Stripe 課金接続＋決勝T課金壁（PR #46・最優先）

**価格確定（6/12・収益最大化）**: 買い切り・早割2段 **JP ¥680→¥980 / 海外 $5→$7**（6/29 0:00 JST 値上げ＝既存 `KNOCKOUT_START_UTC` 流用）。アクセス〜2026-09-30。

**番人実施済（2026-06-13）**: 実装は PR #46。main 追従＆ T-51 競合解消（merge `631d62e`）・**/security-review PASS（ブロッカー無し）**・全491テストPASS・push 済。**T-52（決勝T投票の課金ゲート）は本PRに内包**（KO投票を `isKnockoutStage(current) && !hasKnockoutAccess()` で fail-closed ＋ UI=PaywallLock・グループ投票は無料素通し）。
- ✅**製品判断 確定（2026-06-14）**: 未購入者の決勝T可視範囲は **方針決定セクションの「T-14 決勝T課金マップ」** に確定（無料=ブラケット/アーカイブ/ランキング、有料=カード表示/日付フィルタ/試合詳細/お気に入り/投票/優勝予想の深い機能）。PR #46 は投票ゲート（T-52）のみ＝**残りの壁拡張は T-68 で実装**。

**残＝マージ前提（🧑‍💻 篠田のオーナー作業・PC｜PR #46 実コードと突合済）**: テストモードで先行可（決勝T課金壁は 6/28 まで誰も到達しない＝今テストキーでも実害ゼロ）。
- [ ] **(a) Stripe 商品＋価格4本**: 商品「MatchFav フルアクセス（買い切り）」→ One-time 価格4本。`¥680→STRIPE_PRICE_JP_EARLY` / `¥980→STRIPE_PRICE_JP_REGULAR` / `$5→STRIPE_PRICE_INTL_EARLY` / `$7→STRIPE_PRICE_INTL_REGULAR`
- [ ] **(b) Secret key**: `sk_test_…`（本番化時 `sk_live_…`）→ `STRIPE_SECRET_KEY`
- [ ] **(c) Webhook**: URL `https://matchfav.com/api/stripe/webhook`・イベント **`checkout.session.completed` だけ** → `whsec_…` → `STRIPE_WEBHOOK_SECRET`
- [ ] **(d) Vercel env（Production）**: 上記6つを投入（publishable キーは不要＝サーバリダイレクト型 Checkout）
- [ ] **(e) 本番DB**: migration 0013（entitlements / stripe_processed_events）適用（番人が篠田の GO で実行）
- [ ] **(f) 本番化（〜6/28・5分）**: Stripe をライブモードに切替→価格/secret/webhook をライブで作り直し→ env 差し替え→ Redeploy。
- ※ env 値は番人に渡さない・出力しない。投入後「入れた」と一言で、配線の妥当性確認は手伝える。
- **マージ前に /security-review 必須**（済）。[[launch-monetization-plan]]・[[voting-monetization-model]]・auth-billing 領域。

## ~~T-56 — スマホ画面最適化（PR #63・6/13マージ・本番反映済）~~

マージ済（`e30c405`・squash・本番 matchfav.com 反映確認 HTTP 200）。CSS中心・低リスク・切り戻し容易。改善観点: ①タップ領域 44×44px 以上化（WCAG2.2 Target Size）②隣接ボタン余白で誤タップ防止 ③チーム名の略称過多緩和（短縮コード＋国旗併記）④試合カード（`MatchVersus`/`MatchDayList`）の視認性 ⑤決勝Tブラケット横スクロール体験 ⑥主操作を親指圏（下部）へ ⑦日付フィルタ（`DateFilterBar`）の横はみ出し・タップ性 ⑧順位表（`GroupCard`）の列圧縮 ⑨タイポ/余白の `clamp()` レスポンシブ化。**残**: 篠田の実機 320/375/390 ＋ライト/ダーク最終視覚確認（不満あれば revert で切り戻し）。**T-55（ヘッダー面積）は別PR #62で完了済**。

## T-53 — 各国スカッド「離脱→補充」（注釈②=PR #60 済 / 補充DB①=要GO）

注釈は `src/lib/squad-notes.ts` の `SQUAD_NOTES`（FIFAコード→note）＋ i18n `squad.note<Code>` ×5。`SquadPanel` が末尾表示。**②注釈は PR #60 で実装（6カ国×5言語・本番反映済）**。**① 本番DBへの補充選手反映は未実施**（要GO・partial squad のため注釈で情報は伝達済み＝任意）。⚠️**オランダの FIFAコードは台帳の「NLD」ではなく `NED`**（seed.ts 準拠）。背番号 Senesi/Ljubičić は要確認のため注釈では省略。

| 国 | 離脱選手 | 離脱理由 | 補充選手 | 背番号 |
|---|---|---|---|---:|
| オランダ(**NED**) | Jurrien Timber | 鼠径部負傷 | Lutsharel Geertruida | 2 |
| ドイツ(GER) | Lennart Karl | 左大腿部の筋損傷 | Assan Ouédraogo | 25 |
| アルゼンチン(ARG) | Leonardo Balerdi | 右ふくらはぎ系 | Marcos Senesi | 2 ※要確認 |
| ブラジル(BRA) | Wesley | 左太もも・内転筋 | Éderson（**MF**） | 2 |
| スコットランド(SCO) | Billy Gilmour | 膝負傷 | Tyler Fletcher | 8 |
| オーストリア(AUT) | Christoph Baumgartner | 右太もも負傷 | Dejan Ljubičić | 17 ※要確認 |

⚠️ カナダ/イラク/モロッコ/スウェーデンは不確実なため**入れない**。出典: FIFA・Reuters・Sky Sports。data-squad＋ui-feature/i18n 領域。

## その他 進行中・待ち（詳細）

- **T-43(投票)** みんなの投票ライフサイクル — 実装は PR #55 でマージ済。⚠️**仕様改定2026-06-13**（[[voting-monetization-model]]）: (a)「締切＝初戦KO」→「**次ステージ開始でロック**」に改定（→T-51）、(b)「優勝予想だけ常設」は**廃止**し全ステージ1票ロックに統一、(c) 決勝T課金ゲートは→T-52。
- **T-12** サイト全体の最終動作チェック — 全ページ・全言語を通しで → 2段ゲート → デプロイ。
- **T-8** セキュリティ仕上げ（Sentry / CSP 方針）。作業中・方針判断。
- **T-7** CI（`.github/workflows/ci.yml`）は稼働中だが全 run 赤。落ちるのは `npm audit --audit-level=high` のみ（型/lint/test/build は成功）。high＝esbuild（ビルド/開発依存・本番非該当・実リスク低）。**修正: postcss=PR #59 済＋ esbuild を `npm audit fix`** で緑化。
- **T-29** 購入画面に提供期間〜2026-09-30 を明示（T-14/T-26 実装時にセット）。
- **T-28** 大会汎用化の判断（〜2026-08末）。終了なら 2026-09-30 にデータ削除。今は一般化しない（YAGNI）。

## ✅ 完了（履歴）

- **🔴 T-46（差し戻し→6/13 完了・本番実機OK）** R32再充填バグ — 原因は 0012未適用ではなく `resolveRoundOf32Assignments` の前倒し充填。修正＝`isGroupStageComplete()` ゲート＋ migration 0015 で本番R32再NULL化。本番でスロット表示復帰・round_of_32 投票も閉じた。PR #56・migration 0015 適用済。
- ~~T-45 登録ウォール — 詳細ページをログイン必須化~~（6/13・PR #47 `6b00cc6`。初回2回 main未反映事故→3回目で確実マージ）
- ~~T-43(Wikipedia) イベント補完取込~~（PR #43・6/13実機。注: red=3 はデータ品質要確認余地）
- ~~T-44 公開ブラウズ案A — 閲覧公開・行動だけ要ログイン~~（PR #42。後に T-45 を上に追加が最終形）
- ~~日本代表スカッド手動更新 — 遠藤航→町野修斗（FW・#6・キール）を本番DB反映~~（6/13。スカッド凍結ルールも記録）
- ~~T-13 Clerk(Google)認証~~（`3fe133f`）/ ~~T-23 admin所有者限定・旧PW廃止~~（`42c6178`）/ ~~T-24 会場天気~~（`5d867dc`+`5443a61`）/ ~~T-20a 試合イベント手動tier~~（`2be097a`）/ ~~T-20b 自動tier~~（`dac6893`+`7134845`）/ ~~T-20c 手動tier確認~~（6/11）/ ~~T-21 法務清書~~（`209f24e`）/ ~~T-27 リブランド~~（`209f24e`）/ ~~セキュリティ/品質ゲート~~（`7134845` `23f2d41`）
- ~~T-26 予告バナー(PR #5)~~ / ~~T-32 順位表フラット(PR #6)~~ / ~~T-34 法務簡素化(PR #7)~~ / ~~T-31 A2HSラベル(PR #8)~~ / ~~ヘッダTooltip化(PR #11)~~ / ~~T-36 favicon v5(PR #12)~~ / ~~T-37 ミニヒーロー(PR #14)~~ / ~~T-38 在籍クラブ(PR #16)~~ / ~~T-39 お気に入り一掃(PR #19)~~ / ~~T-33 確定カラー(PR #23)~~ / ~~T-41 favicon v16(PR #27)~~ / ~~T-40 ランキング(PR #28)~~ / ~~T-42 自動取込実証(6/12)~~ / ~~T-22 独自ドメイン(6/12)~~ / ~~T-25 本番Clerk(6/12)~~

## 番号の欠番について

旧セッションの TaskList（T-1〜T-25）は消失し、記録が残るもののみ復元。**T-1〜T-6, T-9〜T-11, T-15〜T-18 は未復元**（完了済か W系吸収の可能性）。番号は再利用しない。

**⚠️ 内容重複の共有メモ（番人⇄Claude 同期・2026-06-13）**: **T-58**（Claude 起票: スマホ最適化第3弾＝①フッター隠し ②お気に入り⭐最左 ほか）と **T-61**（番人 起票: ナビ⭐最左＋フッター隠しバー）は、**同一の篠田フィードバック（フッター隠し／⭐を左）が別々に2番号へ分裂**したもの（番号衝突ではなく**内容重複**）。**両方とも完了・本番反映済で実害なし**。**再発防止**: 番人・Claude とも**新規タスク起票の直前に最新 `git show origin/main:notes/task-ledger.md` を読み**、同一要望を別番号で二重採番しない（既存タスクに追記 or 相互参照で寄せる）。[[task-pr-notation]] と GitHub 一本化ルールに従う。

⚠️ **T-43 は番号衝突**: 台帳の T-43（投票ライフサイクル）と PR #43 が名乗った T-43（Wikipedia補完・完了）の2件。「T-43(投票)」「T-43(Wikipedia)」と区別。新規は最大+1（**T-77〜**）。**使用済**: T-47=ヒーロー文言 / T-48=ADMIN投票リセット(自分票のみ) / T-49=ソフト404 / T-50=勝者色 / T-51=グループ投票窓 / T-52=決勝T課金ゲート / T-53=スカッド離脱→補充 / T-54=SEOブランド発見性 / T-55=スマホヘッダー削減 / T-56=スマホ画面最適化(ボタン/サイズ/タップ領域) / T-57=スマホUI微調整第2弾(実機FB) / T-58=スマホ最適化第3弾(フッター隠し/⭐最左/A2HS控えめ/グループセンタリング) / T-59=ブラケット接続ライン色の均一化 / T-60=A2HS文言を「ショートカット作成」に統一 / T-61=ナビ⭐最左＋フッター隠しバー / T-62=試合結果の取りこぼし原因究明＋再発防止 / T-63=ランキングの同点同順位化 / T-64=日付フィルタ一覧にグループ名表示 / T-65=出場停止を🚫アイコン化 / T-66=右上をミニマム化(≡→⋮＋アバター内包) / T-67=A2HSをショートカット作成の手順案内に統一 / T-68=決勝T課金壁の拡張(カード表示/日付フィルタ/試合詳細/お気に入り/優勝予想深掘りを買い切り壁に・T-14マップ確定分) / T-69=優勝予想の投票に未選択バリデーション/ガイド文言追加 / T-70=優勝予想「WC2026成績のみ」注記を時間軸ニュートラル化 / T-71=優勝予想リストの順位を同点同順位化(T-63の優勝予想版) / T-72=スマホ上部ナビの横スクロール発見性(右端フェード＋半見せ) / T-73=投票締切表示を「移行中/全終了」で出し分け / T-74=migrateランナーのトランザクション化 / T-75=本番500スパイク(00:40UTC /[locale])の事後確認 / T-76=得点ランキングからチェコの得点者(クレイチ)欠落・Yahoo突合で発見。
