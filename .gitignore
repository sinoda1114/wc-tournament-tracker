node_modules
local.db
local.db-*
.next
out
.env
.env.local
.env.*.local
*.tsbuildinfo

# work-summary skill output (local-only, not pushed)
work-summary/

# codegraph local index (per-machine, not pushed)
.codegraph/

# clerk configuration (can include secrets)
/.clerk/

# local AI review artifacts (not committed)
.review-reports/

# local agent/skill tooling (per-developer, not part of the app)
.agents/
.devin/
.windsurf/
.claude/skills/
docs/goals/
skills-lock.json
